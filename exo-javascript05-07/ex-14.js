let lis = [];

function list() {
    for (let index = 1; index <= 50; index++) {
        lis.push(`john.doe.${index}@email.fr`);
       if (`${index}` % 2 === 0) {
        console.log(`john.doe.${index}@email.fr`);
       }
  
    }
  return liste;
}list();
